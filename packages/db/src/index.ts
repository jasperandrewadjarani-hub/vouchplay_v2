/**
 * @vouchplay/db - Supabase DB types and (later) typed query helpers (handover §34.3, §36).
 * Types are hand-authored to match the applied migrations; see ./types.ts for the CLI note.
 */
export const DB_PACKAGE = '@vouchplay/db';
export * from './types';
